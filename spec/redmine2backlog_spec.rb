require 'spec_helper'

describe Redmine2backlog do
  it 'has a version number' do
    expect(Redmine2backlog::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
